import React from 'react';

import './NoData.css';

const nodata = (props) => (
    <article className="NoData">
    {
        // <h1 className="name">No Recent Entries</h1>
    }
            <div className="Member Info">No Recent Entries</div>
    </article>
);

export default nodata;
